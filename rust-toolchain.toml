[toolchain]
# Transitive crates (e.g. `time` 0.3.47, `home` 0.5.12) require rustc 1.88+.
# lab-kit-ferrum pins ferrum-core; if Ferrum raises MSRV, bump this in sync.
channel = "1.88"
components = ["rustfmt", "clippy"]
