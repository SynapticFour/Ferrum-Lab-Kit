[toolchain]
# 1.85+: Cargo can parse crates.io manifests using `edition = "2024"` (e.g. `time` 0.3.47+).
# lab-kit-ferrum still pins ferrum-core; if Ferrum raises MSRV, bump this in sync.
channel = "1.85"
components = ["rustfmt", "clippy"]
