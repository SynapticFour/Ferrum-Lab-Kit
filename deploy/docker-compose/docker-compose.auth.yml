# Unpublished: there is no public synapticfour/ferrum-auth-proxy image or version tag.
# :latest is not a pin — do not invent one. Default path is docker-compose.gateway.yml
# (ghcr.io/synapticfour/ferrum:2bd147c99735866ac9cfb530f62ef984c8efce8b).
# Opt-in only: lab-kit generate compose --legacy-per-service
services:
  auth-proxy:
    image: synapticfour/ferrum-auth-proxy:latest
    networks: [lab]
    ports:
      - "8090:8080"
    environment:
      LAB_KIT_SERVICE: auth
