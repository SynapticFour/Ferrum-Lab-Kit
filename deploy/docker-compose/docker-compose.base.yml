# Always merged first by `lab-kit generate compose`.
#
# Runtime path: monolith `ghcr.io/synapticfour/ferrum` via docker-compose.gateway.yml
# plus FERRUM_SERVICES__ENABLE_* from the selected lab-kit.toml / profile.
# See docs/FERRUM-INTEGRATION.md and .env.example.
name: ferrum-lab-kit

services: {}

networks:
  lab:
    driver: bridge
