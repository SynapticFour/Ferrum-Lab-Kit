{{- if .Values.services.tes.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-tes
  labels:
    {{- include "ferrum-lab-kit.labels" . | nindent 4 }}
    app.kubernetes.io/component: tes
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app.kubernetes.io/component: tes
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-tes
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: tes
  template:
    metadata:
      labels:
        app.kubernetes.io/component: tes
        {{- include "ferrum-lab-kit.labels" . | nindent 8 }}
    spec:
      containers:
        - name: tes
          image: {{ .Values.services.tes.image }}
          ports:
            - containerPort: 8080
{{- end }}
