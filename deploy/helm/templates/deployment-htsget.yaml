{{- if .Values.services.htsget.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-htsget
  labels:
    {{- include "ferrum-lab-kit.labels" . | nindent 4 }}
    app.kubernetes.io/component: htsget
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app.kubernetes.io/component: htsget
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-htsget
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: htsget
  template:
    metadata:
      labels:
        app.kubernetes.io/component: htsget
        {{- include "ferrum-lab-kit.labels" . | nindent 8 }}
    spec:
      containers:
        - name: htsget
          image: {{ .Values.services.htsget.image }}
          ports:
            - containerPort: 8080
{{- end }}
