{{- if .Values.services.trs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-trs
  labels:
    {{- include "ferrum-lab-kit.labels" . | nindent 4 }}
    app.kubernetes.io/component: trs
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app.kubernetes.io/component: trs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-trs
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: trs
  template:
    metadata:
      labels:
        app.kubernetes.io/component: trs
        {{- include "ferrum-lab-kit.labels" . | nindent 8 }}
    spec:
      containers:
        - name: trs
          image: {{ .Values.services.trs.image }}
          ports:
            - containerPort: 8080
{{- end }}
