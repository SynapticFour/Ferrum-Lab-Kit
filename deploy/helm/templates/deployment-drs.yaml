{{- if .Values.services.drs.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-drs
  labels:
    {{- include "ferrum-lab-kit.labels" . | nindent 4 }}
    app.kubernetes.io/component: drs
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app.kubernetes.io/component: drs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ferrum-lab-kit.name" . }}-drs
  labels:
    app.kubernetes.io/component: drs
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: drs
  template:
    metadata:
      labels:
        app.kubernetes.io/component: drs
        {{- include "ferrum-lab-kit.labels" . | nindent 8 }}
    spec:
      containers:
        - name: drs
          image: {{ .Values.services.drs.image }}
          ports:
            - containerPort: 8080
{{- end }}
